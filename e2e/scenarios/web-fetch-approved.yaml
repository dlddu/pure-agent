name: web-fetch-approved
description: "web_fetch 승인 시나리오: Gatekeeper가 요청을 승인하면 HTTP fetch가 정상 수행된다"
level: [1]

# ── Level 1: mock cycle 정의 ──
cycles:
  - export_config:
      linear_issue_id: "mock-issue-id"
      summary: "web_fetch 승인 시나리오 완료."
      actions:
        - "none"
      session_id: "mock-session-web-fetch-approved"
    agent_result: "Done."

# ── 공유 assertions ──
assertions:
  router_decision: "stop"
  export_handler_exit: 0
