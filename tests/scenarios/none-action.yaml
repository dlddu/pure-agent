name: none-action
level: [integration]
run:
  export_config:
    linear_issue_id: "mock-issue-id"
    actions:
      - "none"
    session_id: "mock-session-0"
  agent_result: "Run done."
assertions:
  export_handler_exit: 0
