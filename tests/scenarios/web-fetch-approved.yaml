name: web-fetch-approved
description: "web_fetch_get 승인 시나리오: Gatekeeper가 요청을 승인하면 HTTP fetch가 정상 수행된다"
level: [unit]

# ── Unit: mock run 정의 ──
run:
  export_config:
    summary: "web_fetch_get 승인 시나리오 완료."
    actions:
      - "none"
    session_id: "mock-session-web-fetch-approved"
  agent_result: "Done."

# ── 공유 assertions ──
assertions:
  export_handler_exit: 0
